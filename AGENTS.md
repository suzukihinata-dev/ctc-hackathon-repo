# AGENTS.md

このリポジトリで作業する AI コーディングエージェント（Claude Code、Codex など）への指示。

## 基本ルール

- ユーザーへの返答・コミットメッセージ・ドキュメントは日本語で書く。
- `master` への直接 push は禁止。必ず作業ブランチ（`feature/xxx`、`fix/xxx` など）を切ってプルリクエストを出す。
- プルリクエストのコミットメッセージに `@release` が含まれると、CI（`.github/workflows/auto-merge.yml`）により必須チェック通過後に自動マージされる。確定反映のときだけ使う。

## リポジトリ構成

- `.github/workflows/auto-merge.yml` — `@release` コミットを含む PR の自動マージワークフロー。編集時は PR が open の間のみ実行される点（`pull_request` トリガー）に注意。

## ドキュメント

- `README.md` — プロジェクト概要・セットアップ・開発手順。TODO コメントのある箇所は情報が固まり次第埋める。
- 概要・セットアップ・ディレクトリ構成に変更があったら README を更新する。

## コーディング規約

<!-- TODO: 使用言語・フレームワーク・リンタ・フォーマッタ・テストの方針が決まったらここに書く -->

- 周囲のコードのスタイル（命名、コメント密度、インデント）に合わせる。
- 確立した規約がある場合はここに追記していく。

## 作業フロー

1. `master` から作業ブランチを作成する。
2. 変更をコミットする（日本語で、変更内容を簡潔に記述する）。
3. `master` 向けのプルリクエストを作成する。
4. 自動マージしたい場合はコミットメッセージに `@release` を含める。
